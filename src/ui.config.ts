import icon from './themes/icon'
import type { UiConfig } from './types/global-ui-config'

const config = {
    icon,
    colors: {
        primary: 'purple',
        secondary: 'neutral'
    }
} as UiConfig

export default config
